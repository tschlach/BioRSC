import rhinoscriptsyntax as rs
import Rhino.Geometry as rg
import math 


def getValue(crvRifflePoints):
    for i in crvRifflePoints.riffles:
        if getattr(i, attr) != None:
        	a.append(i.pt)
        	b.append(getattr(i, attr))
    return


a = []
b = []

getValue(crvRifflePoints)

pts = a
values = b


minValue = min(b)
maxValue = max(b)

print(minValue)
print(maxValue)