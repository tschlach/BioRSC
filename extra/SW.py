import rhinoscriptsyntax as rs

class InputVariable(object):
    def __init__(self, name, weight):
        self.name = name
        self.weight = weight

print(Name)
print(Weight)

weighting = InputVariable(Name, Weight)

print(weighting.name, weighting.weight)